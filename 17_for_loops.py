# collection = strings

prices = [10, 20, 30]
total = 0
for dollars in prices: # defining dollars to add up the values in prices.
    total += dollars # total = total + dollars (prices)
print(f"Total: {total}") # formatted string.
